import React from 'react';
import FormQuizEditorLayout from '../components/FormEditor/FormQuizEditorLayout';

const FormEditor: React.FC = () => {
  return <FormQuizEditorLayout mode="campaign" />;
};

export default FormEditor;
